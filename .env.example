# AI provider (Anthropic or Gemini), API key, model, and the GitHub token (raises the GitHub
# API rate limit used for fetching release notes) are all configured from the Settings page in
# the running app now, not here -- see the "AI Provider" panel. This lets you switch providers
# or update a key without a redeploy. (An existing ANTHROPIC_API_KEY/CLAUDE_MODEL/GITHUB_TOKEN
# left over from before this changed will be carried into the database automatically on
# upgrade, but you no longer need to set them here for a fresh install.)

# Path (inside the container) to your compose files, mounted read-only.
# See docker-compose.example.yml for the matching volume mount.
COMPOSE_ROOT=/compose

# Path (inside the container) to the SQLite database file.
DATA_DIR=/data

TZ=Australia/Sydney

# Shared secret Dockhand's generic webhook must send as a query param (?token=...)
# to trigger an immediate out-of-cycle check. Leave blank to disable the webhook endpoint.
WEBHOOK_TOKEN=

# Base URL this app is reachable at, e.g. http://192.168.4.59:8420. Used to make notification
# links clickable. Optional — notifications still work without it, just without a direct link.
PUBLIC_URL=

# How many registries to check at once.
REGISTRY_CHECK_CONCURRENCY=10

# Log health watcher tuning. Everything else — schedules, notifications (Apprise URL,
# on/off, per-feature toggles, severity thresholds), and the lookback window — is configured
# from the Settings tab in the UI, not here. All three features (Updates, Logs, Compose) start off.
LOG_MAX_LINES_PER_CONTAINER=5000
