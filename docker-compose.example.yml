services:
  service-sentinel:
    image: ghcr.io/YOUR_GITHUB_USERNAME/service-sentinel:latest
    container_name: service-sentinel
    restart: unless-stopped
    ports:
      - "8420:8000"
    environment:
      # AI provider, API key, and model are configured from the Settings page in the UI, not
      # here -- see the "AI Provider" panel. Switching providers or updating a key takes
      # effect immediately, no redeploy needed.
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - COMPOSE_ROOT=/compose
      - DATA_DIR=/data
      - TZ=${TZ:-Australia/Sydney}
      - PUBLIC_URL=${PUBLIC_URL}
      - REGISTRY_CHECK_CONCURRENCY=${REGISTRY_CHECK_CONCURRENCY:-10}
      - LOG_LOOKBACK_HOURS=${LOG_LOOKBACK_HOURS:-24}
      - LOG_MAX_LINES_PER_CONTAINER=${LOG_MAX_LINES_PER_CONTAINER:-5000}
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      # Read-only: only needs to list containers and inspect images, never starts/stops/pulls anything.
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # Read-only: point this at wherever Dockge stores your stack compose files.
      - /opt/stacks:/compose:ro
      # Read-write: SQLite database of detected updates and generated notes.
      - service-sentinel-data:/data

volumes:
  service-sentinel-data:
